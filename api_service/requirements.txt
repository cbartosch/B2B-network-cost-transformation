fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
pydantic==2.10.4
httpx==0.28.1
# SPKI pinning: hashing the public key rather than the whole certificate is
# what lets a pin survive a certificate renewal.
cryptography==44.0.0
pytest==8.3.4
