build: 4.30.0
date: 2026-08-24

Verify you have this build before reporting a failure:

    python tests/check_build_config.py

It prints the build number. If the file is missing, or the number differs from
the one you expected, you are running an older extraction - see "Re-extracting"
in README.md.
